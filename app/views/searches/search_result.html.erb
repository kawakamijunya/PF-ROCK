<h2 class="ml-5 my-5" style="border-left: solid 5px #f00; padding: 0.25em 0.5em;">検索結果</h2>
  <div class="ml-5 mr-5">
    <%#検索対象モデルがUserの時 #%>
    <% if @range == "User" %>
      <%= render 'public/users/index',users: @users %>
    <% else %>
    <%#検索対象モデルがUserではない時(= 検索対象モデルがBookの時) #%>
      <%= render 'public/posts/index', posts: @posts %>
    <% end %>
  </div>